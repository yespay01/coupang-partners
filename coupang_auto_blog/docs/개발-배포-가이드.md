# 개발 → 배포 가이드 (Git 기반)

> 최종 업데이트: 2026-02-10
> Git 워크플로우로 개발하고 서버에 배포하는 전체 플로우

---

## 🎯 개발 플로우 개요

```
개발 컴퓨터: 코드 작업 → Git Push
            ↓
      GitHub (원격 저장소)
            ↓
서버 컴퓨터: Git Pull → Docker Build → 배포
```

---

## 💻 1단계: 로컬 개발 (개발 컴퓨터)

### 개발 환경

```bash
# 프로젝트 디렉토리
C:\Users\sakai\OneDrive\바탕 화면\Coupang partnner\coupang_auto_blog

# 웹 앱
cd web

# 로컬 개발 서버 실행
npm run dev
```

### 작업 시작 전 (필수!)

**항상 최신 코드를 받고 시작:**
```bash
# 1. 원격 확인
git fetch

# 2. 상태 확인
git status

# 3. 최신 코드 받기
git pull
```

### 개발 시 주의사항

**✅ 수정 가능:**
- `web/` 디렉토리 내 모든 파일
- `automation-server/` 디렉토리
- `docs/` 문서
- `.env.local` (로컬 개발용)

**❌ 수정 금지:**
- `.env.production` (서버 전용, 서버에서만 수정)
- `node_modules/`
- `.next/` (빌드 결과물)

---

## 🧪 2단계: 로컬 테스트

### 웹 앱 테스트

```bash
cd web

# 개발 서버 실행
npm run dev

# 브라우저에서 확인
http://localhost:3000
```

### 빌드 테스트 (배포 전 필수!)

```bash
# 프로덕션 빌드 테스트
npm run build

# TypeScript 에러 확인
npx tsc --noEmit

# 빌드 결과 실행
npm run start
```

**빌드 에러가 있으면 배포하지 마세요!**

---

## 📤 3단계: Git Commit & Push

### 변경사항 확인

```bash
# 현재 디렉토리로 이동
cd C:\Users\sakai\OneDrive\바탕 화면\Coupang partnner\coupang_auto_blog

# 변경된 파일 확인
git status

# 변경 내용 확인
git diff
```

### 스테이징 & 커밋

```bash
# 모든 변경사항 스테이징
git add .

# 또는 특정 파일만
git add web/app/page.tsx web/components/Header.tsx

# 커밋 (의미 있는 메시지 작성)
git commit -m "fix: 메인 페이지 UI 개선"
```

**커밋 메시지 규칙:**
- `feat:` - 새 기능
- `fix:` - 버그 수정
- `docs:` - 문서 수정
- `style:` - 코드 포맷팅
- `refactor:` - 리팩토링
- `test:` - 테스트 추가

### Push

```bash
# 원격 저장소로 푸시
git push

# 또는 명시적으로
git push origin main
```

---

## 🚀 4단계: 서버 배포 요청

### 서버 측에 통보

서버 컴퓨터의 Claude에게:

```
Git push 완료, 배포 진행해주세요.

변경 내용:
- [수정한 파일 또는 기능 요약]

커밋 ID: [최근 커밋 해시]
```

### 서버 측에서 실행할 명령어 (참고용)

```bash
cd /home/insuk/blog

# Git pull
git pull

# Docker 재빌드 및 배포
docker-compose down
docker-compose build --no-cache
docker-compose up -d

# 로그 확인
docker-compose logs -f
```

---

## ✅ 5단계: 배포 확인

### 서버 응답 확인

서버 측에서 다음 결과를 받아야 함:

```
✅ 배포 완료
- Git pull: 성공 (XX개 파일 업데이트)
- Docker build: 성공
- 서비스: 정상 실행 중
- https://semolink.store 접속: 정상
```

### 브라우저에서 확인

```
https://semolink.store
```

**확인 사항:**
- [ ] 페이지가 로드되는가?
- [ ] UI가 깨지지 않았는가?
- [ ] 변경사항이 반영되었는가?
- [ ] 콘솔 에러가 없는가?

### 관리자 페이지 확인

```
https://semolink.store/admin
```

---

## 📋 배포 시나리오별 가이드

### 시나리오 1: UI/컴포넌트 수정

**변경:**
- `web/components/AdminDashboard.tsx` 수정

**절차:**
```bash
# 1. 수정 및 테스트
npm run dev

# 2. 빌드 테스트
npm run build

# 3. Commit & Push
git add web/components/AdminDashboard.tsx
git commit -m "fix: 관리자 대시보드 UI 개선"
git push

# 4. 서버에 배포 요청
```

---

### 시나리오 2: 페이지 추가

**변경:**
- `web/app/new-page/page.tsx` 추가

**절차:**
```bash
# 1. 파일 생성 및 테스트
npm run dev

# 2. Commit & Push
git add web/app/new-page/
git commit -m "feat: 새 페이지 추가"
git push

# 3. 서버에 배포 요청
```

---

### 시나리오 3: 설정 파일 수정

**변경:**
- `next.config.ts` 수정
- `docker-compose.yml` 수정

**주의:** 설정 파일 변경은 서버 재빌드 필수!

**절차:**
```bash
# 1. 수정 및 빌드 테스트
npm run build

# 2. Commit & Push
git add next.config.ts docker-compose.yml
git commit -m "fix: Next.js 설정 변경"
git push

# 3. 서버에 "재빌드 필수" 명시하여 배포 요청
```

---

### 시나리오 4: 의존성 추가

**변경:**
- `npm install new-package`
- `package.json`, `package-lock.json` 변경됨

**절차:**
```bash
# 1. 패키지 설치
cd web
npm install new-package

# 2. 테스트
npm run build

# 3. Commit & Push
git add package.json package-lock.json
git commit -m "deps: new-package 추가"
git push

# 4. 서버에 배포 요청 (Docker가 자동으로 npm ci 실행)
```

---

### 시나리오 5: automation-server 수정

**변경:**
- `automation-server/src/routes/collect.js` 수정

**절차:**
```bash
# 1. 수정 및 테스트

# 2. Commit & Push
git add automation-server/
git commit -m "fix: 상품 수집 로직 개선"
git push

# 3. 서버에 배포 요청
```

---

## ⚠️ 배포 시 주의사항

### 1. 반드시 빌드 테스트

**배포 전:**
```bash
cd web
npm run build
```

**에러 있으면 절대 배포하지 말 것!**

### 2. TypeScript 에러 확인

```bash
npx tsc --noEmit
```

### 3. 의존성 충돌 확인

```bash
npm ci  # package-lock.json 기준 설치
```

### 4. 환경변수 확인

**로컬 (.env.local):**
```env
ADMIN_GUARD_BYPASS=true
NODE_ENV=development
```

**서버 (.env.production):**
```env
ADMIN_GUARD_BYPASS=false
NODE_ENV=production
```

**서버 환경변수를 변경하려면:**
- 서버 Claude에게 요청
- 서버에서만 `.env.production` 수정 가능

---

## 🔄 작업 중 충돌 해결

### 다른 컴퓨터에서 작업한 경우

```bash
# 1. Pull 시도
git pull

# 2. 충돌 발생 시
# 방법 A: 현재 작업 임시 저장
git stash
git pull
git stash pop

# 방법 B: 현재 작업 커밋 후 병합
git add .
git commit -m "WIP: 작업 중"
git pull
# 충돌 해결
git add .
git commit -m "merge: 충돌 해결"
git push
```

---

## 🆘 배포 실패 시

### 빌드 실패

**서버 응답:**
```
❌ Docker build 실패
Error: Cannot find module 'xxx'
```

**해결:**
1. `package.json`에 의존성 추가
2. 커밋 & 푸시
3. 재배포 요청

---

### 런타임 에러

**증상:**
- 서버에서 "컨테이너 재시작 반복"
- `docker logs`에 에러

**해결:**
1. 서버에서 로그 확인 요청
2. 에러 분석
3. 코드 수정
4. 커밋 & 푸시
5. 재배포

---

### Git 충돌

**증상:**
```
CONFLICT (content): Merge conflict in web/app/page.tsx
```

**해결:**
```bash
# 1. 충돌 파일 확인
git status

# 2. 파일 열어서 충돌 해결
# <<<<<<< HEAD
# =======
# >>>>>>> 표시 찾아서 수동 병합

# 3. 해결 후 커밋
git add .
git commit -m "merge: 충돌 해결"
git push
```

---

## 📊 배포 소요 시간

| 단계 | 시간 |
|------|------|
| Git pull | 5-10초 |
| Docker 빌드 | 3-5분 |
| 컨테이너 시작 | 10-20초 |
| **전체** | **4-6분** |

---

## 🎯 빠른 참조

### 일반적인 작업 흐름

```bash
# 1. 작업 시작
git pull

# 2. 코드 수정
# ...

# 3. 테스트
npm run dev
npm run build

# 4. Commit & Push
git add .
git commit -m "feat: 새 기능"
git push

# 5. 서버에 배포 요청
```

### 자주 쓰는 Git 명령어

```bash
# 상태 확인
git status

# 변경 내용 확인
git diff

# 최신 코드 받기
git pull

# 커밋 히스토리
git log --oneline -10

# 특정 파일 되돌리기
git checkout -- 파일명

# 마지막 커밋 취소 (아직 push 안 한 경우)
git reset --soft HEAD~1
```

---

## 📝 체크리스트

### 배포 전

- [ ] `git pull`로 최신 코드 받음
- [ ] 로컬에서 `npm run build` 성공
- [ ] TypeScript 에러 없음 (`npx tsc --noEmit`)
- [ ] 변경사항 로컬 테스트 완료
- [ ] 불필요한 console.log 제거
- [ ] Git commit & push 완료

### 배포 후

- [ ] 서버에서 "배포 완료" 응답 받음
- [ ] https://semolink.store 접속 확인
- [ ] 변경사항 반영 확인
- [ ] 브라우저 콘솔 에러 없음
- [ ] 관리자 페이지 정상 작동

---

## 📞 커뮤니케이션 템플릿

### 일반 배포 요청

```
Git push 완료, 배포 진행해주세요.

변경 내용:
- web/app/page.tsx: 메인 페이지 UI 개선
- web/components/Header.tsx: 헤더 스타일 수정

커밋 ID: a1b2c3d
```

### 긴급 배포 요청

```
🚨 긴급 배포 필요

문제: [버그 설명]
수정: [수정 내용]
커밋: a1b2c3d

즉시 배포 부탁드립니다.
```

### 롤백 요청

```
🚨 긴급 롤백 필요

문제: [문제 설명]
이전 커밋으로 롤백: b2c3d4e

서버에서 다음 실행:
git reset --hard b2c3d4e
docker-compose down && docker-compose up -d --build
```

---

## 🔐 보안 주의사항

1. **절대 커밋하지 말 것:**
   - `.env.production`
   - `.env.local`
   - Firebase 키 파일
   - API 비밀 키

2. **.gitignore 확인:**
   ```
   .env.local
   .env.production
   node_modules/
   .next/
   ```

3. **민감한 정보는 환경변수로:**
   - 코드에 하드코딩 금지
   - 환경변수 사용

---

## 📖 관련 문서

- `CLAUDE.md` - Claude 작업 지침 (전체 워크플로우)
- `docs/서버-운영-지침.md` - 서버 운영 가이드
- `docs/서버-Claude-체크리스트.md` - 서버 배포 체크리스트
- `docs/프로젝트-구조.md` - 프로젝트 구조 설명
- `README.md` - 프로젝트 개요

---

**마지막 업데이트:** 2026-02-10 (Git 기반 워크플로우로 전환)
