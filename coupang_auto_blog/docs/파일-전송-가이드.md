# 파일 전송 가이드

## 📍 서버 정보

```
서버 주소:
  - 내부망: 192.168.0.5
  - Tailscale: 100.107.201.37

사용자: insuk
배포 경로: /home/insuk/blog/
```

---

## 🚀 방법 1: SCP로 직접 전송 (추천)

### Windows PowerShell에서 실행

```powershell
# 1. web 디렉토리로 이동
cd "C:\Users\sakai\OneDrive\바탕 화면\Coupang partnner\coupang_auto_blog\web"

# 2. SCP로 전송
# 내부망에서:
scp -r ./* insuk@192.168.0.5:/home/insuk/blog/

# 또는 Tailscale을 통해:
scp -r ./* insuk@100.107.201.37:/home/insuk/blog/
```

**비밀번호 입력 후 전송 시작됩니다.**

### 전송 확인

전송 완료 후 서버에서 확인:

```bash
ssh insuk@192.168.0.5
# 또는
ssh insuk@100.107.201.37

# 디렉토리 확인
cd /home/insuk/blog
ls -la
```

**예상 출력:**
```
Dockerfile
docker-compose.yml
.dockerignore
.env.production
nginx-semolink.conf
deploy-server.sh
package.json
next.config.ts
app/
components/
...
```

---

## 🚀 방법 2: rsync로 전송 (더 효율적)

rsync는 변경된 파일만 전송하므로 업데이트 시 유용합니다.

### Git Bash 또는 WSL에서 실행

```bash
# web 디렉토리로 이동
cd "/c/Users/sakai/OneDrive/바탕 화면/Coupang partnner/coupang_auto_blog/web"

# rsync로 전송 (내부망)
rsync -avz --progress ./ insuk@192.168.0.5:/home/insuk/blog/

# 또는 Tailscale
rsync -avz --progress ./ insuk@100.107.201.37:/home/insuk/blog/
```

**옵션 설명:**
- `-a`: 아카이브 모드 (권한 보존)
- `-v`: 상세 출력
- `-z`: 압축 전송
- `--progress`: 진행 상황 표시

---

## 🚀 방법 3: Git 사용 (버전 관리)

### GitHub에 push 후 서버에서 clone

#### 로컬에서:

```bash
# Git 저장소 초기화 (아직 안 했다면)
git init
git add .
git commit -m "Initial commit for server deployment"

# GitHub 저장소 생성 후
git remote add origin https://github.com/your-username/coupang-blog.git
git push -u origin main
```

#### 서버에서:

```bash
ssh insuk@192.168.0.5

cd /home/insuk/blog
git clone https://github.com/your-username/coupang-blog.git .

# 또는 이미 있다면
git pull
```

**장점:**
- 버전 관리
- 업데이트 간편 (`git pull`)
- 변경 이력 추적

**주의:**
- `.env.production`은 `.gitignore`에 추가하고 서버에서 직접 생성

---

## ✅ 전송 후 체크리스트

전송 완료 후 서버에서 확인:

```bash
# 1. SSH 접속
ssh insuk@192.168.0.5

# 2. 디렉토리 이동
cd /home/insuk/blog

# 3. 필수 파일 확인
ls -la Dockerfile docker-compose.yml .env.production deploy-server.sh

# 4. 디렉토리 구조 확인
ls -la app/ components/ hooks/ stores/ public/

# 5. package.json 확인
cat package.json | grep '"name"'
```

**모든 파일이 있으면 다음 단계로 진행 ✅**

---

## 🔄 업데이트 방법

코드 수정 후 재전송:

### SCP 사용 시:
```powershell
cd "C:\Users\sakai\OneDrive\바탕 화면\Coupang partnner\coupang_auto_blog\web"
scp -r ./* insuk@192.168.0.5:/home/insuk/blog/
```

### rsync 사용 시 (변경된 파일만):
```bash
rsync -avz --progress ./ insuk@192.168.0.5:/home/insuk/blog/
```

### Git 사용 시:
```bash
# 로컬
git add .
git commit -m "Update code"
git push

# 서버
ssh insuk@192.168.0.5
cd /home/insuk/blog
git pull
docker-compose down
docker-compose up -d --build
```

---

## ❓ 문제 해결

### 권한 거부 (Permission denied)

```bash
# 서버에서 권한 확인
ls -la /home/insuk/blog

# 소유자 변경 (필요시)
sudo chown -R insuk:insuk /home/insuk/blog
```

### SSH 키 인증 설정 (비밀번호 입력 생략)

```bash
# 로컬에서 SSH 키 생성 (이미 있으면 스킵)
ssh-keygen -t rsa -b 4096

# 서버에 공개 키 복사
ssh-copy-id insuk@192.168.0.5

# 또는
ssh-copy-id insuk@100.107.201.37
```

이후 비밀번호 없이 접속 가능!

---

## 📞 다음 단계

파일 전송 완료 후:

1. ✅ 서버 측 Claude에게 `docs/서버-Claude-체크리스트.md` 전달
2. ✅ Firebase Admin SDK 키 설정
3. ✅ 배포 스크립트 실행
4. ✅ https://semolink.store 확인

**파일 전송을 시작하세요!** 🚀
