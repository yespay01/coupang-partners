# 개발 → 배포 가이드

> 로컬에서 개발하고 서버로 배포하는 전체 플로우

---

## 🎯 개발 플로우 개요

```
로컬 개발 → 테스트 → 파일 전송 → 서버 배포 → 확인
```

---

## 💻 1단계: 로컬 개발

### 개발 환경

```bash
# 프로젝트 디렉토리
C:\Users\sakai\OneDrive\바탕 화면\Coupang partnner\coupang_auto_blog

# 웹 앱
cd web

# 로컬 개발 서버 실행
npm run dev
```

### 개발 시 주의사항

**✅ 수정 가능:**
- `web/` 디렉토리 내 모든 파일
- `functions/` 디렉토리 (Firebase Functions)
- `docs/` 문서
- `.env.local` (로컬 개발용)

**❌ 수정 금지:**
- `.env.production` (서버 전용)
- `node_modules/`
- `.next/` (빌드 결과물)

---

## 🧪 2단계: 로컬 테스트

### 웹 앱 테스트

```bash
cd web

# 개발 서버 실행
npm run dev

# 브라우저에서 확인
http://localhost:3000
```

### 빌드 테스트

```bash
# 프로덕션 빌드 테스트
npm run build

# 빌드 결과 확인
npm run start
```

### Firebase Functions 테스트

```bash
cd functions

# Functions 로컬 실행
firebase emulators:start --only functions
```

---

## 📤 3단계: 서버로 파일 전송

### 방법 A: 전체 전송 (큰 변경 시)

```bash
cd web

# tar로 압축하면서 SSH로 전송
tar --exclude='node_modules' \
    --exclude='.next' \
    --exclude='.env.local' \
    --exclude='tsconfig.tsbuildinfo' \
    -czf - . | ssh insuk@192.168.0.5 "cd /home/insuk/blog && tar -xzf -"
```

### 방법 B: 개별 파일 전송 (작은 변경 시)

```bash
cd web

# 단일 파일
scp 파일명 insuk@192.168.0.5:/home/insuk/blog/

# 여러 파일
scp file1.tsx file2.ts insuk@192.168.0.5:/home/insuk/blog/app/

# 디렉토리
scp -r components/ insuk@192.168.0.5:/home/insuk/blog/
```

### 방법 C: 특정 파일들만 전송

```bash
cd web

# 자주 수정하는 파일들
scp next.config.ts Dockerfile package.json insuk@192.168.0.5:/home/insuk/blog/

# 특정 페이지
scp app/page.tsx insuk@192.168.0.5:/home/insuk/blog/app/

# 컴포넌트
scp -r components/ insuk@192.168.0.5:/home/insuk/blog/
```

---

## 🚀 4단계: 서버 배포 요청

### 서버 측 Claude에게 전달

```
배포 준비 완료

변경 내용:
- [수정한 파일 목록]

배포 진행해주세요.
```

### 서버 측에서 실행할 명령어 (참고용)

```bash
cd /home/insuk/blog

# 배포 실행
docker stop coupang-blog && docker rm coupang-blog
docker build --no-cache -t blog-coupang-blog .
docker run -d \
  --name coupang-blog \
  --restart always \
  -p 3000:3000 \
  --env-file .env.production \
  blog-coupang-blog

# 로그 확인
docker logs coupang-blog -f
```

---

## ✅ 5단계: 배포 확인

### 서버 응답 확인

서버 측에서 다음 결과를 받아야 함:

```
✅ 배포 완료 - 컨테이너 정상 실행 중
```

### 브라우저에서 확인

```
https://semolink.store
```

**확인 사항:**
- [ ] 페이지가 로드되는가?
- [ ] UI가 깨지지 않았는가?
- [ ] 변경사항이 반영되었는가?
- [ ] 콘솔 에러가 없는가?

### 관리자 페이지 확인

```
https://semolink.store/admin
```

---

## 🔄 6단계: Git 커밋

배포 성공 후 로컬 변경사항을 Git에 저장:

```bash
cd C:\Users\sakai\OneDrive\바탕 화면\Coupang partnner\coupang_auto_blog

# 변경사항 확인
git status

# 스테이징
git add .

# 커밋
git commit -m "feat: [변경 내용 요약]"

# 푸시 (원격 저장소 있는 경우)
git push
```

---

## 📋 배포 시나리오별 가이드

### 시나리오 1: UI/컴포넌트 수정

**변경:**
- `components/AdminDashboard.tsx` 수정

**전송:**
```bash
cd web
scp components/AdminDashboard.tsx insuk@192.168.0.5:/home/insuk/blog/components/
```

**배포:** 서버 측에 배포 요청

---

### 시나리오 2: 페이지 추가

**변경:**
- `app/new-page/page.tsx` 추가

**전송:**
```bash
cd web
scp -r app/new-page insuk@192.168.0.5:/home/insuk/blog/app/
```

**배포:** 서버 측에 배포 요청

---

### 시나리오 3: 설정 파일 수정

**변경:**
- `next.config.ts` 수정
- `Dockerfile` 수정

**전송:**
```bash
cd web
scp next.config.ts Dockerfile insuk@192.168.0.5:/home/insuk/blog/
```

**배포:** 서버 측에 배포 요청 (반드시 재빌드 필요)

---

### 시나리오 4: 의존성 추가

**변경:**
- `npm install new-package`
- `package.json`, `package-lock.json` 변경됨

**전송:**
```bash
cd web
scp package.json package-lock.json insuk@192.168.0.5:/home/insuk/blog/
```

**배포:** 서버 측에 배포 요청 (npm ci 실행됨)

---

### 시나리오 5: 전체 재배포

**변경:**
- 여러 파일 대량 수정

**전송:**
```bash
cd web
tar --exclude='node_modules' --exclude='.next' --exclude='.env.local' -czf - . | \
  ssh insuk@192.168.0.5 "cd /home/insuk/blog && tar -xzf -"
```

**배포:** 서버 측에 배포 요청

---

## 🔧 Firebase Functions 배포

웹 앱과 별도로 Firebase Functions 배포:

```bash
cd C:\Users\sakai\OneDrive\바탕 화면\Coupang partnner\coupang_auto_blog

# Functions 배포
firebase deploy --only functions

# 특정 함수만 배포
firebase deploy --only functions:generateReview

# 로그 확인
firebase functions:log
```

---

## ⚠️ 배포 시 주의사항

### 1. 환경변수 확인

**로컬 (.env.local):**
```env
ADMIN_GUARD_BYPASS=true
NODE_ENV=development
```

**서버 (.env.production):**
```env
ADMIN_GUARD_BYPASS=false
NODE_ENV=production
```

### 2. 빌드 에러 확인

로컬에서 반드시 빌드 테스트:
```bash
npm run build
```

### 3. 의존성 충돌 확인

```bash
npm ci  # package-lock.json 기준 설치
```

### 4. TypeScript 에러 확인

```bash
npx tsc --noEmit
```

---

## 🆘 배포 실패 시

### 빌드 실패

**에러:**
```
Error: Cannot find module 'xxx'
```

**해결:**
1. `package.json`에 의존성 추가
2. 파일 전송 후 재배포

---

### 런타임 에러

**증상:**
- 컨테이너가 계속 재시작됨
- `docker logs`에 에러

**해결:**
1. 서버에서 로그 확인 요청
2. 환경변수 확인
3. 코드 수정 후 재배포

---

### CSS 안 로드

**증상:**
- UI가 깨짐
- 스타일 없음

**해결:**
1. `.next/static` 디렉토리 확인
2. Dockerfile의 COPY 명령 확인
3. 전체 재빌드

---

## 📊 배포 통계

### 예상 소요 시간

| 단계 | 시간 |
|------|------|
| 파일 전송 (전체) | 30초 - 1분 |
| 파일 전송 (일부) | 5-10초 |
| Docker 빌드 | 3-5분 |
| 컨테이너 시작 | 10-20초 |
| **전체** | **4-7분** |

---

## 🎯 빠른 참조

### 자주 쓰는 명령어

```bash
# 전체 파일 전송
cd web && tar --exclude='node_modules' --exclude='.next' -czf - . | ssh insuk@192.168.0.5 "cd /home/insuk/blog && tar -xzf -"

# 단일 파일 전송
cd web && scp 파일명 insuk@192.168.0.5:/home/insuk/blog/

# Firebase Functions 배포
firebase deploy --only functions

# Git 커밋
git add . && git commit -m "메시지" && git push
```

---

## 📝 체크리스트

### 배포 전

- [ ] 로컬에서 `npm run build` 성공
- [ ] TypeScript 에러 없음
- [ ] ESLint 경고 확인
- [ ] 변경사항 테스트 완료
- [ ] 불필요한 console.log 제거
- [ ] .env.local 변경사항 없음 (있다면 서버 .env.production 업데이트 필요)

### 배포 후

- [ ] 서버에서 "배포 완료" 응답 받음
- [ ] https://semolink.store 접속 확인
- [ ] 변경사항 반영 확인
- [ ] 브라우저 콘솔 에러 없음
- [ ] 관리자 페이지 정상 작동
- [ ] Git 커밋 완료

---

## 📞 커뮤니케이션 템플릿

### 배포 요청

```
배포 준비 완료

변경 내용:
- app/page.tsx - 메인 페이지 UI 개선
- components/Header.tsx - 헤더 스타일 수정

배포 진행해주세요.
```

### 긴급 롤백 요청

```
🚨 긴급 롤백 필요

문제: [문제 설명]
증상: [사용자가 보는 증상]

이전 버전으로 롤백 부탁드립니다.
```

---

**마지막 업데이트:** 2026-02-03
