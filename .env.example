# ===========================
# Root Compose ENV Example (설치형/테스트 스택 공통)
# docker-compose.yml 의 포트/컨테이너/볼륨명 기본값 제어
# ===========================

# 스택 이름 prefix (컨테이너명/볼륨명에 사용)
STACK_NAME=coupang

# DB / MinIO root credentials (compose base 파일에서 사용)
POSTGRES_PASSWORD=your-secure-password
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin123

# Host port bindings (운영/테스트 동시 실행 시 분리)
POSTGRES_HOST_PORT=5433
MINIO_API_HOST_PORT=9000
MINIO_CONSOLE_HOST_PORT=9001
WEB_HOST_PORT=3000
AUTOMATION_HOST_PORT=4000
